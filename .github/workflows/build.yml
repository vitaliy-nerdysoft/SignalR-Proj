name: Build
on: [push]
jobs:
  Build-workflow:
    runs-on: windows-latest
    steps:
      - name: Build
        run: dotnet build "SignalRSample.sln" -c Release
      - name: Tests
        run: dotnet test --no-build -c Release